/**
 * @deepseek-ai/dsh-beardy — the Beardy agent profile bundle. The package's
 * substance is `cordis.patch.yml`; this module carries no runtime API.
 * @module @deepseek-ai/dsh-beardy
 */

export {}
